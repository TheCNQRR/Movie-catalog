<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.MovieCatalog" parent="Theme.Material3.Dark.NoActionBar">
        <item name="materialCalendarTheme">@style/ThemeOverlay.MovieCatalog.DatePicker</item>
    </style>


    <style name="ThemeOverlay.MovieCatalog.DatePicker"
        parent="ThemeOverlay.Material3.MaterialCalendar">
        <item name="colorPrimary">@color/accent</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorOnSurface">@color/white</item>
        <item name="android:colorBackground">@color/date_picker_background</item>
    </style>
</resources>